import React from "react";
import ShowTask from "./ShowTask";
export const metadata = {
  title: "ShowTask : Work Manager",
};

const page = () => {
  return (
    <div>
      <ShowTask />
    </div>
  );
};

export default page;
