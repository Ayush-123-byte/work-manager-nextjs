import React from "react";
import Login from "./Login";
export const metadata = {
  title: "Login: work Manager",
};

const page = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default page;
